<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Socket vers l'adbd du casque lui-même (127.0.0.1) et découverte mDNS locale. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Repérer localement les jeux VR, y compris les anciens titres (research.md R5). -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Appli 2D (panneau) : pas de suivi de tête requis. -->
    <uses-feature
        android:name="android.hardware.vr.headtracking"
        android:required="false" />

    <!-- allowBackup=false et dataExtractionRules : la clé ADB privée ne doit jamais quitter le casque
         (principe I, FR-027), ni par sauvegarde ni par transfert vers un autre appareil (Android 12+). -->
    <application
        android:name=".OqtApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenQuestTuner">

        <meta-data
            android:name="com.oculus.supportedDevices"
            android:value="quest2|questpro|quest3|quest3s" />

        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Taille par défaut du panneau sur Horizon OS (research.md R4). -->
            <layout
                android:defaultWidth="1024dp"
                android:defaultHeight="640dp" />
        </activity>
    </application>
</manifest>
